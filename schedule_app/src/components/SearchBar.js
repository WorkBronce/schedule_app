import React from 'react';
import searchIcon from '../images/search.png'; // Adjust the path based on your project structure

function SearchButton() {
    return (
      <button>
        <img src={searchIcon} alt="Search" />
      </button>
    );
  }
  

export default SearchButton;
